services:
  proof-server:
    image: "ghcr.io/midnight-ntwrk/proof-server:9.0.0-rc.3"
    container_name: "faucet-test-proof-server"
    ports:
      - 6300
    environment:
      RUST_BACKTRACE: "full"
  indexer:
    image: "ghcr.io/midnight-ntwrk/indexer-standalone:4.3.2"
    container_name: "faucet-test-indexer"
    ports:
      - "8088:8088"
    environment:
      RUST_LOG: "indexer=warn,chain_indexer=warn,indexer_api=warn,wallet_indexer=warn,indexer_common=warn,fastrace_opentelemetry=off,info"
      APP__INFRA__SECRET: "303132333435363738393031323334353637383930313233343536373839303132"
      APP__INFRA__NODE__URL: "ws://node:9944"
      APP__APPLICATION__NETWORK_ID: "undeployed"
      APP__INFRA__STORAGE__PASSWORD: "faucet-test"
      APP__INFRA__PUB_SUB__PASSWORD: "faucet-test"
      APP__INFRA__LEDGER_STATE_STORAGE__PASSWORD: "faucet-test"
      APP__INFRA__SPO_NODE__URL: "ws://node:9944"
      APP__INFRA__SPO_NODE__BLOCKFROST_ID: "e2e-test-dummy"
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:8088/ready"]
      interval: "5s"
      timeout: "5s"
      retries: 36
      start_period: 60s
    depends_on:
      node:
        condition: service_healthy
  node:
    image: "ghcr.io/midnight-ntwrk/midnight-node:2.0.0-rc.3"
    container_name: "faucet-test-node"
    ports:
      - "9944:9944"
    healthcheck:
      test:
        - "CMD-SHELL"
        - >
          curl -fs -H 'Content-Type: application/json' -d
          '{"id":1,"jsonrpc":"2.0","method":"chain_getBlockHash","params":[1]}' http://localhost:9944 | grep -q
          '"result":"0x'
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 5s
    environment:
      CFG_PRESET: "dev"
      SIDECHAIN_BLOCK_BENEFICIARY: "04bcf7ad3be7a5c790460be82a713af570f22e0f801f6659ab8e84a52be6969e"
